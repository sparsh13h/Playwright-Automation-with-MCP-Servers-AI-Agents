export class CheckoutOverviewPageLocators {
  static readonly summaryInfo = '.summary_info';
  static readonly summaryInfoLabel = '.summary_info_label';
  static readonly paymentInfoLabel = '.summary_value_label';
  static readonly itemName = '.inventory_item_name';
  static readonly itemDescription = '.inventory_item_desc';
  static readonly itemPrice = '.inventory_item_price';
  static readonly subtotalLabel = '.summary_subtotal_label';
  static readonly taxLabel = '.summary_tax_label';
  static readonly totalLabel = '.summary_total_label';
  static readonly finishButton = 'button[data-test="finish"]';
  static readonly cancelButton = 'button[data-test="cancel"]';
}
